task: make notes only according to the given instructions and if you have a strong sense of adding somthing to it then only you are allowed to add the content

for every algo:
1. algorithm jist: what type of algorithm it is, how it works and what are the best case to use this algo.
2. worst, average and worst case complexity analysis: using recurrence relation and RAM model. (step by step solving of the relation for their complexities)
2.1. every line in the algorithm must have a cost associated to it c_1, c_2, .... c_n.
2.2. the numbers in the start of every line in the algorithm is used in explaining the components of reccurence relation and complexity analysis.
3. give reasons for every algo to be stable (or not), inplace (or not) and adaptive (or not).
4. an example that will visually demonstrate the working of this algo for each iteration. you have to show every changes to the array and the variables in each loop.
5. prove the Loop invariants and the correctness of an algorithm

insertion sort algo{
INSERTION-SORT(A, n)
1    for i = 2 to n
2        key = A[i]
3        // Insert A[i] into the sorted subarray A[1:i-1].
4        j = i - 1
5        while j > 0 and A[j] > key
6            A[j+1] = a[j]
7            j = j-1
8        A[j+1] = key
}

selection sort algo{
SELECTION-SORT(A, n)
1  for i = 1 to n - 1
2      min_index = i
3      for j = i + 1 to n
4          if A[j] < A[min_index]
5              min_index = j
6      exchange A[i] with A[min_index]
}

merge sort algo{
MERGE(A,p,q,r)
1    n_L = q - p + 1                                        // length of A[p:q]
2    n_R = r - q                                            // length of A[q + 1:r]
3    let L[0:n_L - 1] and R[0:n_R - 1] be new arrays
4    for i = 0 to n_L - 1                                   // copy A[p:q] into L[0:n_L - 1]
5        L[i] = A[p + i]
6    for j = 0 to n_R - 1                                   // copy A[q + 1:r] into R[0:n_R - 1]
7        R[j] = A[q + j + 1]
8    i = 0                                                  // i indexes the smallest remaining element in L
9    j = 0                                                  // j indexes the smallest remaining element in R
10   k = p                                                  // k indexes the location in A to fill
11   // As long as each of the arrays L and R contains an unmerged elements, copy the smallest unmerged element back into A[p:r].
12   while i < n_L and j < n_R
13       if L[i] <= R[j]
14           A[k] = L[i]
15           i = i + 1
16       else A[k] = R[j]
17           j = j + 1
18       k = k + 1
19   // Having gone through one of the L and R entirely, copy the remainder of the order to the end of A[p:r].
20   while i < n_L
21       A[k] = L[i]
22       i = i + 1
23       k = k + 1
24   while j < n_R
25       A[k] = R[j]
26       j = j + 1
27       k = k + 1


MERGE-SORT(A,p,r)
1    if p <= r                                  // zero or one element?
2        return
3    q = floor((p + r)/2)                       // midpoint of A[p:r]
4    MERGE-SORT(A,p,q)                          // recursively sort A[p:q]
5    MERGE-SORT(A,q + 1,r)                      // recursively sort A[q + 1:r]
6    // Merge A[p:q] and A[q+1:r] into A[p:r].
7    MERGE(A,p,q,r)
}

bubble sort algo{
BUBBLESORT(A,n)
    for i = 1 to n - 1
        for j = n downto i + 1
            if A[j] < A[j - 1]
                exchange A[j] with A[j - 1]
}

heapsort algo{
HEAPSORT(A,n)
1    BUILD-MAX-HEAP(A,n)
2    for i = n downto 2
3        exchange A[1] with A[i]
4        A.heap-size = A.heap-size - 1
5        MAX-HEAPIFY(A,1)
}

quick sort algo{
QUICKSORT(A,p,r)
1    if p < r
2        // Partition the subarray around the pivot, which ends up in A[q].
3        q = PARTITION(A,p,r)
4        QUICKSORT(A,p,q-1) // recursively sort the low side
5        QUICKSORT(A,q+1,r) // recursively sort the high side

PARTITION(A,p,r)
1    x = A[r]                            // the pivot
2    i = p-1                             // highest index into the low side
3    for j = p to r - 1                  // process each element other than the pivot
4        if A[j] <= x                    // does this element belong on the low side?
5           i = i + 1                    // index of a new slot in the low side
6            exchange A[i] with A[j]     // put this element there
7    exchange A[i+1] with A[r]           // pivot goes just to the right of the low side
8    return i + 1                        // new index of the pivot
}

randomized quick sort algo{
RANDOMIZED-PARTITION(A,p,r)
1    i = RANDOM(p,r)
2    exchange A[r] with A[i]
3    return PARTITION(A,p,r)

RANDOMIZED-QUICKSORT(A,p,r)
1    if p < r
2        q = RANDOMIZED-PARTITION(A,p,r)
3        RANDOMIZED-QUICKSORT(A,p,q-1)
4        RANDOMIZED-QUICKSORT(A,q+1,r)
}

count sort algo{
COUNTING-SORT(A,n,k)
1    let B[1:n] and C[0:k] be new arrays
2    for i = 0 to k
3        C[i] = 0
4    for j = 1 to n
5        C[A[j]] = C[A[j]] + 1
6    // C[i] now contains the number of elements equal to i.
7    for i = 1 to k
8        C[i] = C[i] + C[i - 1]
9    // C[i] now contains the number of elements less than or equal to i.
10   // Copy A to B , starting from the end of A.
11   for j = n downto 1
12       B[C[A[j]]] = A[j]
13       C[A[j]] = C[A[j]] - 1     // to handle duplicate values
14   return B
}

minimum element algo{
MINIMUM(A,n)
1    min = A[1]
2    for i = 2 to n
3        if min > A[i]
4            min = A[i]
5    return min
}

min-max element algo{
SIMULTANEOUS-MIN-MAX(A, n)
1    if n is odd
2        min = A[1]
3        max = A[1]
4        i = 2
5    else
6        if A[1] < A[2]
7            min = A[1]
8            max = A[2]
9        else
10           min = A[2]
11           max = A[1]
12       i = 3
13   while i <= n - 1
14       if A[i] < A[i+1]
15           if A[i] < min
16               min = A[i]
17           if A[i+1] > max
18               max = A[i+1]
19       else
20           if A[i+1] < min
21               min = A[i+1]
22           if A[i] > max
23               max = A[i]
24       i = i + 2
25   return (min, max)
}

strassen's algo{
MATRIX-ADD(A, B)
1   n = A.rows
2   let C be a new n x n matrix
3   for i = 1 to n
4       for j = 1 to n
5           C[i, j] = A[i, j] + B[i, j]
6   return C

MATRIX-SUB(A, B)
1   n = A.rows
2   let C be a new n x n matrix
3   for i = 1 to n
4       for j = 1 to n
5           C[i, j] = A[i, j] - B[i, j]
6   return C

STRASSEN(A, B)
1   n = A.rows
2   let C be a new n x n matrix
3   if n == 1
4       C[1, 1] = A[1, 1] * B[1, 1]
5       return C
6   partition A into n/2 x n/2 submatrices A11, A12, A21, A22
7   partition B into n/2 x n/2 submatrices B11, B12, B21, B22
8   S1 = MATRIX-SUB(B12, B22)
9   S2 = MATRIX-ADD(A11, A12)
10  S3 = MATRIX-ADD(A21, A22)
11  S4 = MATRIX-SUB(B21, B11)
12  S5 = MATRIX-ADD(A11, A22)
13  S6 = MATRIX-ADD(B11, B22)
14  S7 = MATRIX-SUB(A12, A22)
15  S8 = MATRIX-ADD(B21, B22)
16  S9 = MATRIX-SUB(A11, A21)
17  S10 = MATRIX-ADD(B11, B12)
18  P1 = STRASSEN(A11, S1)
19  P2 = STRASSEN(S2, B22)
20  P3 = STRASSEN(S3, B11)
21  P4 = STRASSEN(A22, S4)
22  P5 = STRASSEN(S5, S6)
23  P6 = STRASSEN(S7, S8)
24  P7 = STRASSEN(S9, S10)
25  C11 = MATRIX-ADD(MATRIX-SUB(MATRIX-ADD(P5, P4), P2), P6)
26  C12 = MATRIX-ADD(P1, P2)
27  C21 = MATRIX-ADD(P3, P4)
28  C22 = MATRIX-SUB(MATRIX-SUB(MATRIX-ADD(P5, P1), P3), P7)
29  combine C11, C12, C21, C22 into matrix C
30  return C
}
